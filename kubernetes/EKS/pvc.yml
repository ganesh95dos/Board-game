apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: h2-pvc
  namespace: board-game
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ebs-sc

